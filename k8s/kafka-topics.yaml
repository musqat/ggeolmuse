---
apiVersion: batch/v1
kind: Job
metadata:
  name: kafka-topics-init
  labels:
    app: kafka-topics-init
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: kafka-topics-init
    spec:
      restartPolicy: OnFailure
      containers:
        - name: kafka-topics-creator
          image: confluentinc/cp-kafka:7.6.0
          command:
            - sh
            - -c
            - |
              echo "Waiting for Kafka to be ready..."
              sleep 30

              echo "Creating Kafka topics..."

              # Trading topics
              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.trade.completed \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000 \
                --config segment.bytes=1073741824

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.trade.failed \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.trade.cancelled \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.dividend.received \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.holdings.updated \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              # Market data topics
              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.price.updated \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=86400000 \
                --config cleanup.policy=compact

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.asset.created \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.dividend.updated \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.fxrate.updated \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.collection.completed \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              # User account topics
              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.email.send \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.account.balance.updated \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.login.success \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.login.failed \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=604800000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.account.deposit.completed \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.account.withdrawal.completed \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              # Dead Letter Queue topics
              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.trade.completed.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.trade.failed.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.trade.cancelled.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.dividend.received.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic trading.holdings.updated.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.price.updated.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.asset.created.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic market.dividend.updated.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.email.send.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.account.balance.updated.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.login.success.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.login.failed.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.account.deposit.completed.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              kafka-topics --create --if-not-exists \
                --bootstrap-server kafka:9092 \
                --topic user.account.withdrawal.completed.DLT \
                --partitions 3 \
                --replication-factor 1 \
                --config retention.ms=2592000000

              echo "Listing all topics:"
              kafka-topics --list --bootstrap-server kafka:9092

              echo "Kafka topics initialization completed!"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
