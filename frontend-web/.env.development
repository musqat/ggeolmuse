# vite dev 서버로 띄울 때만 쓴다.
#
# 도커 이미지는 빌드 인자(Dockerfile 의 VITE_API_URL)로 값을 넣지만,
# npm run dev 는 그 경로를 안 타서 비어 있게 된다. 그러면 요청이 /api 없이
# /market/... 으로 나가 vite 프록시(/api -> 게이트웨이)에 안 걸린다.
VITE_API_URL=/api
