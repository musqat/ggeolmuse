{{- if .Values.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ggeolmuse.fullname" . }}-ingress
  labels:
    {{- include "ggeolmuse.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
    {{- if .Values.ingress.customAnnotations }}
    {{- toYaml .Values.ingress.customAnnotations | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
  {{- range .Values.ingress.hosts }}
  - host: {{ .host }}
    http:
      paths:
      {{- range .paths }}
      {{- if not .backend.namespace }}
      - path: {{ .path }}
        pathType: {{ .pathType }}
        backend:
          service:
            name: {{ .backend.service }}
            port:
              number: {{ .backend.port }}
      {{- else }}
      - path: {{ .path }}
        pathType: {{ .pathType }}
        backend:
          service:
            name: {{ .backend.service }}-proxy
            port:
              number: {{ .backend.port }}
      {{- end }}
      {{- end }}
  {{- end }}
{{- end }}
