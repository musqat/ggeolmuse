name: CI
on:
  pull_request:
  push:
    branches: [ master ]
jobs:
  build-common:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v3
      - name: Build & Publish common-lib
        run: |
          cd common-lib
          chmod +x ./gradlew
          ./gradlew clean test build publishToMavenLocal --no-daemon

  build-services:
    runs-on: ubuntu-latest
    needs: build-common
    strategy:
      matrix:
        service:
          - config-server
          - gateway-server
          - user-service
          - trade-service
          - market-data-service
          - backtest-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v3
      - name: Build & Publish common-lib (for dependencies)
        run: |
          cd common-lib
          chmod +x ./gradlew
          ./gradlew publishToMavenLocal --no-daemon
      - name: Build & Test ${{ matrix.service }}
        run: |
          cd ${{ matrix.service }}
          chmod +x ./gradlew
          ./gradlew clean test build --no-daemon
